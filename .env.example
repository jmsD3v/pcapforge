# Google Gemini API key — enables the AI incident narrative / kill-chain
# reconstruction in `pcapforge analyze`. Without it, pcapforge still parses
# the PCAP and runs all detectors normally; it just skips the AI section
# (fails gracefully, no crash).
# Get a free-tier key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
